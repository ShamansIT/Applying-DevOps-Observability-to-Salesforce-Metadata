# Real-org testing runbook

Operator steps - `org:check` -> `readiness:org` -> `pilot:freeze-plan` -> `pilot:org` - and the offline
commands are in [EXPERIMENT.md](EXPERIMENT.md). Real runs are the operator's; nothing is fabricated.
