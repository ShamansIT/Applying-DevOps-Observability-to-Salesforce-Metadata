{
  "repeats": 2,
  "_comment": "Main benchmark. Add S02+ as { scenario, groundTruth } pairs. Must be disjoint from pilot (S01); eval:main and eval:repeat refuse an empty or pilot-overlapping set.",
  "cases": [
    { "scenario": "fixtures/scenarios/S02.json", "groundTruth": "fixtures/ground-truth/S02.json" },
    { "scenario": "fixtures/scenarios/S03.json", "groundTruth": "fixtures/ground-truth/S03.json" },
    { "scenario": "fixtures/scenarios/S04.json", "groundTruth": "fixtures/ground-truth/S04.json" }
  ]
}
