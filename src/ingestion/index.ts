// Ingestion - read-only metadata sources: org snapshot capture, the Tooling client,
// the MetadataComponentDependency client, the Flow and Apex parsers, and the local
// Salesforce DX project reader. Every call goes through the read-only wrapper.
export {};
